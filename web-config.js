// The app is served by the same server as the API, so no address is needed here.
// Only set API_BASE if you host the API on a different domain (then enable CORS on the server).
export const CONFIG = { API_BASE: "" };
